---
title: 🚀 Getting Started
description: Set up your first Zuno app from scratch using CMake and FetchContent.
---

import { Code } from '@astrojs/starlight/components';

Zuno is a modern C++ web framework designed for performance, modularity, and developer ergonomics. This guide walks you through setting up a minimal Zuno project using **CMake** and **FetchContent**.

---

## 🧰 Prerequisites

Before you begin, make sure you have the following installed:

- **C++20 compatible compiler** (e.g. GCC ≥ 10, Clang ≥ 12, MSVC ≥ 2019)
- **CMake ≥ 3.20**
- **Git** (for FetchContent)

---

## 📁 Project Structure

Create a new folder for your project:

<Code lang="bash" code={`mkdir zuno-hello && cd zuno-hello`} />

Inside, create the following structure:

<Code lang="plaintext" code={`zuno-hello/
├── CMakeLists.txt
└── main.cpp`} />

---

## ⚙️ CMake Configuration

Use `FetchContent` to download and integrate Zuno automatically:

<Code title="CMakeLists.txt" lang="cmake" code={`cmake_minimum_required(VERSION 3.20)
project(zuno-hello)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

include(FetchContent)

FetchContent_Declare(
  zuno
  GIT_REPOSITORY https://github.com/ZunoFramework/zuno.git
  GIT_TAG        main  # Or use a specific version tag
)

FetchContent_MakeAvailable(zuno)

add_executable(zuno-hello main.cpp)
target_link_libraries(zuno-hello PRIVATE zuno::zuno)`} />

---

## 📝 Hello World in C++

Now create your `main.cpp` file with a basic server:

<Code title="main.cpp" lang="cpp" code={`#include <zuno/zuno.hpp>

int main() {
  zuno::App app;

  app.get("/", [](auto req, auto res) {
    res.send("Hello, world from Zuno!");
  });

  app.listen(8080);
}`} />

---

## 🛠️ Build and Run

From the root of your project:

<Code lang="bash" code={`cmake -B build
cmake --build build
./build/zuno-hello`} />

Then open your browser and visit [http://localhost:8080](http://localhost:8080) — you should see:

<Code lang="plaintext" code={`Hello, world from Zuno!`} />

---

## ✅ You're Up and Running!

You now have your first Zuno server live. From here, you can:

- Add more routes
- Integrate middleware
- Enable TLS
- Modularize your app

👉 Continue to the [Architecture Overview](/docs/architecture) to understand how Zuno works under the hood.
