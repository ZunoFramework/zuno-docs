---
title: Hola Mundo
description: Tu primera aplicación Zuno, un servidor HTTP mínimo en C++ moderno.
locale: es
---
import { Code } from '@astrojs/starlight/components'

# 👋 Hola Mundo

Construyamos tu primera aplicación con Zuno: un servidor HTTP mínimo que responde con un saludo. Este ejemplo demuestra lo simple que es comenzar con Zuno.

---

## 🧱 Estructura del Proyecto

<Code
  lang="text"
  code={`hello-world/
├── CMakeLists.txt
└── main.cpp`}
/>

---

## 📄 `CMakeLists.txt`

<Code
  lang="cmake"
  code={`cmake_minimum_required(VERSION 3.20)
project(hello_world)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

include(FetchContent)

FetchContent_Declare(
  zuno
  GIT_REPOSITORY https://github.com/zuno-framework/zuno.git
  GIT_TAG        main
)

FetchContent_MakeAvailable(zuno)

add_executable(hello-world main.cpp)
target_link_libraries(hello-world PRIVATE Zuno::Zuno)`}
/>

---

## 📄 `main.cpp`

<Code
  lang="cpp"
  code={`#include &lt;zuno/zuno.hpp&gt;

int main() {
  zuno::App app;

  app.get("/", [](Request& req, Response& res) {
    res.send("¡Hola desde Zuno!");
  });

  app.listen(3000);
}`}
/>

---

## 🚀 Ejecutar la Aplicación

<Code
  lang="bash"
  code={`cmake -B build
cmake --build build
./build/hello-world`}
/>

Deberías ver:

<Code
  lang="text"
  code={`Servidor Zuno ejecutándose en http://localhost:3000`}
/>

Visita [http://localhost:3000](http://localhost:3000) en tu navegador y verás:

<Code lang="text" code={`¡Hola desde Zuno!`} />


---

## 🧭 Próximos Pasos

Ahora que construiste tu primera app con Zuno, prueba:

- [Ruteo modular](/es/guide/routing)
- [Extensiones de middleware](/es/extensions/compression)
- [Soporte TLS](/es/guide/tls-security)

---

Zuno te permite pasar de “Hola Mundo” a APIs listas para producción—sin el exceso de código.
